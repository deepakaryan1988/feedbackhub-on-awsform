# Use exact versions for better reproducibility
save-exact=true

# Use npm for package management
package-manager=npm

# Disable audit for CI/CD (optional)
audit=false

# Set registry (optional, uses default npm registry)
# registry=https://registry.npmjs.org/ 